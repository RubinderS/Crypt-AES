function getHelp(): string {
  return `Node-Crypt
  Usage:-
  --help -> help
  --verison -> version

  encrypt:
    --source, -s: source of file or directory to be encrypted 
    --password, -s: source of file or directory to be encrypted 


  `;
}

export {getHelp};
